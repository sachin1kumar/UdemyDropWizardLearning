password: pass

server:
  applicationConnectors:
    - type: http
      port: 8080
    - type: https
      port: 8443
      keyStorePath: udemydropwizard.keystore
      keyStorePassword: password
      validateCerts: false


database:
   driverClass: org.postgresql.Driver
   user: postgres
   password:
   url: jdbc:postgresql://localhost:5432/udemyBookmarks